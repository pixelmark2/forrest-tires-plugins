jQuery(document).ready(function($) {
	$('.carousel-list').slick({
	  dots: false,
	  infinite: true,
	  speed: 500,
	  fade: false,
	  cssEase: 'linear',
	  prevArrow: '<span></span>',
	  nextArrow: '<span></span>'
	});
})